# scs-schema

Static schema data and manifest generator for SCS-Intellisense Systems. This repository stores JSON schema files under `data/schemas`, validtes them, and produces a `data/manifest.json` that references each schema by a **publich CDN URL** (jsDelivr) using a **commit SHA**.
